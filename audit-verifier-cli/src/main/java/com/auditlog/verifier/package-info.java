/**
 * Offline bundle verifier. Depends on {@code audit-hashing-core} only — never on {@code
 * audit-service} — so a recipient runs the same hash construction the service ran.
 */
package com.auditlog.verifier;
