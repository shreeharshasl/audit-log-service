/**
 * Application use cases and {@code @Transactional} boundaries. Calls the hashing core and
 * repositories. Never imports a DTO or {@code HttpServletRequest}.
 */
package com.auditlog.service.service;
